#include <stdio.h>
int main()
{
    int a, b, input;
    printf("input: "); scanf("%d", &input);
    for(a = 1; a <= input; a++)
    {
        for(b = 1; b <= a ; b++)
        {
            printf("%c", b+64);
        }
        printf("\n");
    }
    return 0;
}
